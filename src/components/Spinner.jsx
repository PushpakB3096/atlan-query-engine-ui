// Spinner is just a static component that displays a loading bar. This can be changed later to an SVG.
const Spinner = () => {
  return <div>Loading data. Please wait...</div>;
};

export default Spinner;
